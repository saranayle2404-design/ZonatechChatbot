const messages=document.querySelector("#messages"),form=document.querySelector("#chat-form"),input=document.querySelector("#message");
const sessionId=localStorage.getItem("zonatech_session")||crypto.randomUUID();localStorage.setItem("zonatech_session",sessionId);
function addMessage(text,sender,quickReplies=[]){const wrapper=document.createElement("article");wrapper.className=`message ${sender}`;const paragraph=document.createElement("p");paragraph.textContent=text;wrapper.append(paragraph);if(quickReplies.length){const buttons=document.createElement("div");buttons.className="quick-replies";quickReplies.forEach(label=>{const button=document.createElement("button");button.type="button";button.textContent=label;button.addEventListener("click",()=>sendMessage(label));buttons.append(button)});wrapper.append(buttons)}messages.append(wrapper);messages.scrollTop=messages.scrollHeight}
async function sendMessage(message){const cleanMessage=message.trim();if(!cleanMessage)return;addMessage(cleanMessage,"user");input.value="";try{const response=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:cleanMessage,session_id:sessionId})}),data=await response.json();addMessage(data.reply||data.error||"Ocurrió un error inesperado.","bot",data.quick_replies)}catch{addMessage("No pude conectarme con el servidor local. Intenta recargar la página.","bot")}}
form.addEventListener("submit",event=>{event.preventDefault();sendMessage(input.value)});sendMessage("hola");
